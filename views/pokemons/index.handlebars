<section>
  <h1>Pokemons</h1>
  {{#each Pokemons}}  
    <article class="pokemon">
      <h2><a href="/pokemons/{{_id}}">{{title}}</a></h2>
      <p>{{body}}</p>
    </article>
  {{/each}}
</section>
